diff --git a/qisa-as/CMakeLists.txt b/qisa-as/CMakeLists.txt
index 2c3dfac..2a25e51 100644
--- a/qisa-as/CMakeLists.txt
+++ b/qisa-as/CMakeLists.txt
@@ -1,6 +1,6 @@
 project(qisa-as CXX)

-cmake_minimum_required(VERSION 3.1)
+cmake_minimum_required(VERSION 3.10)

 IF (CMAKE_CXX_COMPILER_ID STREQUAL "GNU")
   SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")
@@ -72,39 +72,3 @@ set_property(TARGET qisa-as qisa-as-lib
              PROPERTY CXX_STANDARD 14)


-# We use Swig to expose the assembler driver interface to Python
-
-FIND_PACKAGE(SWIG REQUIRED)
-INCLUDE(${SWIG_USE_FILE})
-
-FIND_PACKAGE(PythonLibs REQUIRED)
-INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH})
-
-SET(CMAKE_SWIG_FLAGS "")
-SET(CMAKE_SWIG_OUTDIR "${CMAKE_LIBRARY_OUTPUT_DIRECTORY}")
-
-SET_SOURCE_FILES_PROPERTIES(qisa-as-swig.i PROPERTIES CPLUSPLUS ON)
-SET_SOURCE_FILES_PROPERTIES(qisa-as-swig.i PROPERTIES SWIG_FLAGS "-includeall")
-SWIG_ADD_MODULE(pyQisaAs python qisa-as-swig.i)
-SWIG_LINK_LIBRARIES(pyQisaAs ${PYTHON_LIBRARIES} qisa-as-lib)
-
-# Install section.
-IF (QISA_AS_INSTALL_FOR_SETUP_PY)
-  # Tailored for installation using setup.py, that installs the python
-  # libraries in an intermediate staging area.
-  # Note that the qisa-as executable is intentionally not installed.
-  INSTALL(TARGETS qisa-as-lib ${SWIG_MODULE_pyQisaAs_REAL_NAME}
-    DESTINATION ${CMAKE_INSTALL_PREFIX})
-  INSTALL(FILES "${CMAKE_CURRENT_BINARY_DIR}/pyQisaAs.py" DESTINATION ${CMAKE_INSTALL_PREFIX})
-
-ELSE (QISA_AS_INSTALL_FOR_SETUP_PY)
-
-  # Normal installation.
-  INSTALL(TARGETS qisa-as qisa-as-lib ${SWIG_MODULE_pyQisaAs_REAL_NAME}
-    ARCHIVE DESTINATION lib
-    LIBRARY DESTINATION lib
-    RUNTIME DESTINATION bin)
-
-  INSTALL(FILES "${CMAKE_CURRENT_BINARY_DIR}/pyQisaAs.py"
-    DESTINATION lib)
-ENDIF (QISA_AS_INSTALL_FOR_SETUP_PY)
diff --git a/qisa-as/main.cpp b/qisa-as/main.cpp
index 07b1f67..8df1dda 100644
--- a/qisa-as/main.cpp
+++ b/qisa-as/main.cpp
@@ -171,7 +171,7 @@ main(const int argc, const char **argv)

   QISA::QISA_Driver driver;

-  //driver.read();
+  if (const char* layout = std::getenv("QISA_LAYOUT")) driver.read(layout);
   driver.enableScannerTracing(enableTrace);
   driver.enableParserTracing(enableTrace);
   driver.setVerbose(enableVerbose);
diff --git a/qisa-as/qisa_driver.cpp b/qisa-as/qisa_driver.cpp
index 7a06fe2..e12e603 100644
--- a/qisa-as/qisa_driver.cpp
+++ b/qisa-as/qisa_driver.cpp
@@ -1,3 +1,4 @@
+#include <limits>
 #include <fstream>
 #include <sstream>
 #include <iomanip>
@@ -159,7 +160,7 @@ QISA_Driver::read(std::string input_filename)
     int             i = 0;
     int             qubit_num = 0;
     int             num_edge_address = 0;
-    bool read_qubit_num, read_edge_list, read_num_nn_edge = false;
+    bool read_qubit_num = false, read_edge_list = false, read_num_nn_edge = false;

     //input_filename = "D:\\Projects\\QuMA_Sim\\scripts\\quantum_layout_information.txt";

diff --git a/qisa-as/qisa_driver.h b/qisa-as/qisa_driver.h
index f14ed49..e3b9b7b 100644
--- a/qisa-as/qisa_driver.h
+++ b/qisa-as/qisa_driver.h
@@ -1347,7 +1347,7 @@ private: // -- variables
     bool
     operator() (const QISA::location& lhs, const QISA::location& rhs) const
     {
-      if (lhs.begin == rhs.begin)
+      if (lhs.begin.filename == rhs.begin.filename && lhs.begin.line == rhs.begin.line && lhs.begin.column == rhs.begin.column)
       {
         return ((lhs.end.line < rhs.end.line) ||
                 ((lhs.end.line == rhs.end.line) && (lhs.end.column < rhs.end.column)));
diff --git a/qisa-as/qisa_parser.yy b/qisa-as/qisa_parser.yy
index e820824..dbcd09a 100644
--- a/qisa-as/qisa_parser.yy
+++ b/qisa-as/qisa_parser.yy
@@ -10,6 +10,7 @@
  // This code is put in the parser's header file.
 %code requires
 {
+  #include <cstdint>
   #include <memory>
   #include <vector>
